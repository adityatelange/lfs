kind: pipeline
name: lfs

platform:
  os: linux
  arch: amd64

clone:
  depth: 1
  
  
steps:
  - name: init
    image: ubuntu:latest	
    commands:
      - chmod -R +x /drone/src/
      - /drone/src/install_requirements.sh
      - /drone/src/version-check.sh
      - /drone/src/environment_config.sh
